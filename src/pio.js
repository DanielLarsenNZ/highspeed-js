/** Peripheral Input/Output
 *  * Provides communication to the input (switch) busses and output (solenoids, lights).
 *  * multiplexes lights and solenoid IO based into matrices, e.g. 8x8 matrix for light switching.
 *  * demultiplexes swith input based on a matrix.
 *  * No I2C or peripheral specific code in here.
 * 
 */
module.export = function(){
  "define strict";
  
};